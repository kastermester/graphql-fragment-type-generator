{
  "name": "graphql-fragment-type-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --harmony --es-staging --no-colors --recursive test",
    "lint": "tslint --project tsconfig.json src/*.ts src/**/*.ts test/*.ts test/**/*.ts",
    "build": "rm -rf lib && tsc",
    "prepublish": "npm test && npm run lint && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "graphql": "^0.9.1"
  },
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/graphql": "^0.8.6",
    "@types/mocha": "^2.2.39",
    "@types/node": "^7.0.5",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "ts-node": "^2.1.0",
    "tslint": "^4.4.2",
    "typescript": "^2.1.5"
  }
}
