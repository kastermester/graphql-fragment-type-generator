{
  "name": "ts-graphql-fragment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "./mocha.sh",
    "lint": "tslint --project tsconfig.json src/*.ts src/**/*.ts test/*.ts test/**/*.ts",
    "build": "rm -rf lib && tsc",
    "prepublish": "npm test && npm run lint && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "graphql": "^0.9.1"
  },
  "devDependencies": {
    "@types/graphql": "^0.8.6",
    "@types/node": "^7.0.4",
    "mocha": "^3.2.0",
    "tslint": "^4.4.2",
    "typescript": "^2.1.5"
  }
}
